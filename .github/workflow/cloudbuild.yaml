name: Cypress test suite using action
on: [push]
job:
  cypress-run:
  runs-on: ubuntu-latest
  steps:
    -name: Checkout
    uses: actions/checkout@v3
    -name: cypress run
    uses: cypress-io/github-action@v5
